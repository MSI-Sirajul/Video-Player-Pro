<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.videoplayer">

    <!-- পারমিশন সমূহ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Video Player"
        android:theme="@style/Theme.VideoPlayer">
        
        <!-- মেইন লঞ্চার অ্যাক্টিভিটি -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- প্লেয়ার অ্যাক্টিভিটি (আপডেট করা হয়েছে) -->
        <activity android:name=".PlayerActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:launchMode="singleTop"
            android:theme="@style/Theme.VideoPlayer.FullScreen"
            android:exported="true"> <!-- এটি true করা হয়েছে যাতে বাইরের অ্যাপ এক্সেস করতে পারে -->

            <!-- ভিডিও প্লেয়ার হিসেবে ডিটেক্ট করার জন্য ইনটেন্ট ফিল্টার -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <!-- সাপোর্টেড স্কিম (ফাইল এবং কনটেন্ট ইউআরআই) -->
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                
                <!-- সাপোর্টেড ভিডিও টাইপ -->
                <data android:mimeType="video/*" />
                <data android:mimeType="application/mp4" />
                <data android:mimeType="application/x-mpegurl" />
                <data android:mimeType="application/vnd.apple.mpegurl" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>
        <activity android:name=".FolderActivity" android:screenOrientation="portrait"/>

        <!-- ব্যাকগ্রাউন্ড প্লেব্যাক সার্ভিস -->
        <service android:name=".PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false">
        </service>
    </application>
</manifest>